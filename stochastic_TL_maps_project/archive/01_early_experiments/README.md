# Early Experiments

First runs of Bellhop through MATLAB before the structured UQ pipeline.

## Files

| File | Description |
|------|-------------|
| `main.m` | Simplest possible wrapper: calls `simpleBellhopHazat` with hard-coded frequency, depth, SVP type, and plots the TL field. The original "hello world" for the project. |
| `var for uniform.fig` | MATLAB figure file showing variance map from early uniform-depth Monte Carlo (pre-pipeline). |
| `uniform_depth_28_4.zip` | ZIP snapshot of the entire `uniform_depth_28_4/` project at a stable checkpoint (~April 2024). |

## Status
Reference only — superseded by `2026-05-26/` pipeline.
